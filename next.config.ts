import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['fakestoreapi.com', 'images.unsplash.com', 'cdn.dummyjson.com'],
  },
};

export default nextConfig;
